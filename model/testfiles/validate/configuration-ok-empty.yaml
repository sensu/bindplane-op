apiVersion: bindplane.observiq.com/v1
kind: Configuration
metadata:
  name: macos
  labels:
    platform: macos
    app: cabin
spec:
  raw: ""
  sources:
  destinations:
  selector:
    matchLabels:
      "configuration": macos
